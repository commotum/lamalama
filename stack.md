## Verdict

The website is a **custom WordPress build**, not a React/Next.js, Vue/Nuxt, Webflow, Framer, or Elementor site.

The most accurate description of the stack is:

> **WordPress/PHP + custom `lamalama2025` theme + Tailwind CSS + a Vite-style JavaScript build + GSAP/WebGL + Swup + Lenis**

I inspected the complete-page archive you supplied. 

## Evidence

| Layer                  | Finding                                             | Confidence |
| ---------------------- | --------------------------------------------------- | ---------: |
| CMS/backend            | WordPress with custom PHP templates                 |  Very high |
| Theme                  | Bespoke theme named `lamalama2025`                  |  Very high |
| CSS                    | Tailwind CSS with a custom design system            |  Very high |
| Build tooling          | Most likely Vite/Rollup                             |       High |
| Front-end architecture | Server-rendered HTML enhanced by modular JavaScript |       High |
| Animation              | GSAP and CSSPlugin                                  |  Very high |
| Page transitions       | Swup                                                |  Very high |
| Smooth scrolling       | Lenis                                               |  Very high |
| Graphics               | Custom WebGL image/video components                 |  Very high |
| Localization           | WPML                                                |  Very high |
| Optimization/caching   | WP Rocket                                           |  Very high |

### 1. Definitive WordPress fingerprints

The actual site body contains:

```html
<body class="home wp-singular
  page-template
  page-template-components
  page-template-templates
  page-template-home
  page-template-componentstemplateshome-php
  page page-id-7
  wp-theme-lamalama2025">
```

Those `page`, `page-id-7`, `page-template-*`, and `wp-theme-*` classes are generated by WordPress’s `body_class()` system. The `...home-php` class also points to a PHP page template. ([WordPress Developer Resources][1])

Its assets are served from paths such as:

```text
/wp-content/themes/lamalama2025/dist/assets/...
/wp-content/themes/lamalama2025/dist/fonts/...
/wp-content/themes/lamalama2025/dist/icons/...
```

That establishes the active custom theme name as **`lamalama2025`**.

### 2. It is a bespoke theme, not a page builder

The page uses a custom component convention:

```html
data-component="parts/img"
data-component="parts/mono_text_reveal"
data-component="parts/buttons/button"
data-component="blocks/case_item"
data-component="blocks/backdrop_theme"
data-component="sections/hero_extended"
data-component="parts/webgl_image"
```

I counted **207 elements with `data-component` attributes**, representing 17 distinct component types.

There were no characteristic markers for:

* Elementor
* Divi
* Webflow
* Framer
* React or Next.js
* Vue or Nuxt
* Svelte
* Angular

There is also no hydration payload such as `__NEXT_DATA__`, `_next/`, `data-reactroot`, `data-v-*`, or a JavaScript framework root. Instead, WordPress renders the complete page HTML and JavaScript progressively enhances specific components.

### 3. Tailwind CSS

The site stylesheet contains Tailwind’s preflight/reset output, extensive `--tw-*` variables, and utility classes such as:

```text
w-[calc(...)]
gap-y-10
text-textPrimary
bg-bgSecondary
aspect-[109/174]
scroll-px-5
```

The square-bracket arbitrary-value syntax and utility-class structure are characteristic of Tailwind. ([Tailwind CSS][2])

They have heavily customized it with their own design tokens and component classes, including:

```text
ll-display-rg
ll-heading-1
ll-grid
ll-scroller
ll-part--wysiwyg
ll-contact-form
```

So it is not “stock Tailwind”; it is a custom design system built on Tailwind.

### 4. Most likely Vite for bundling

The JavaScript is delivered as ES modules with hashed chunks:

```html
<script type="module" src="main-DPWgAreT.js"></script>

<link rel="modulepreload" href="button-DVBIAmlK.js">
<link rel="modulepreload" href="gsap-DSgSnfEt.js">
<link rel="modulepreload" href="hero_extended-BYa0q2PX.js">
```

The saved main entry contains:

```js
import "./scripts-B95NYudC.js";
```

Hashed assets, ES-module entry points, automatic chunking, and module-preload links are strongly consistent with a **Vite production build**, likely using its underlying Rollup bundler. Vite specifically supports hashed production assets and manifests intended for integration with server-rendered backends such as WordPress. ([vitejs][3])

I would call this **Vite with high confidence**, although the archive did not include the original `package.json` or Vite configuration, so direct Rollup or another Rollup-based setup cannot be completely excluded.

### 5. Motion and interaction stack

The archive contains explicit GSAP chunks:

```text
gsap-DSgSnfEt.js
CSSPlugin-BUyJDoRP.js
gsapConfig-CK2e0qRV.js
```

The current Awwwards listing for this version of the Lama Lama website also identifies **GSAP, JavaScript, and WebGL** as its principal technologies. ([Awwwards][4])

Swup is identifiable from:

```html
<html class="swup-enabled">
<a data-no-swup>
<a data-swup-morph="...">
<div data-swup-scroll-container>
```

`data-no-swup` is an official Swup navigation control attribute. ([swup][5])

Lenis is identifiable from:

```html
<div class="ll-scroller js-scroller lenis">
```

Lenis is the smooth-scrolling layer, which is commonly used to synchronize DOM scrolling with animated and WebGL content. ([Lenis][6])

There are also dedicated bundles and DOM hooks for:

```text
webgl_image-BQ952JvA.js
webgl_video-CkQF5wqV.js
js-webgl-image
js-webgl-video
js-canvas
```

That indicates custom WebGL rendering rather than purely CSS-based effects. The exact WebGL engine—such as Three.js, OGL, or an in-house renderer—cannot be established from the saved files.

### 6. WordPress plugins visible in the capture

The generated HTML explicitly identifies:

```html
<meta name="generator" content="WPML ver:4.9.6 stt:37,1;">
<meta name="generator" content="WP Rocket 3.23.1.1">
```

So, in this capture:

* **WPML 4.9.6** handles English/Dutch localization.
* **WP Rocket 3.23.1.1** handles caching and front-end optimization.

Other integrations visible include Google Tag Manager, Cookiebot, Fathom-related analytics modules, and a Cal.com scheduling embed.

## One misleading artifact in the archive

The saved HTML also contains:

```html
<bettercampus-components>
<bettercanvas-portal>
/*! tailwindcss v4.1.18 */
```

That markup is injected by a browser extension using WXT and a shadow DOM. It is **not part of Lama Lama’s website**. Therefore, `Tailwind CSS v4.1.18` should not be treated as the site’s Tailwind version.

The site itself unquestionably uses Tailwind, but the exact Tailwind version cannot be established from this browser capture.

## Final classification

**Primary framework/platform:** WordPress
**Implementation:** Bespoke componentized PHP theme
**Styling:** Tailwind CSS
**Bundler:** Most likely Vite/Rollup
**Front end:** Custom modular JavaScript rather than React/Vue
**Interactive stack:** GSAP, Swup, Lenis, and custom WebGL components
**Theme name:** `lamalama2025`

[1]: https://developer.wordpress.org/themes/classic-themes/templates/page-template-files/?utm_source=chatgpt.com "Page Templates – Theme Handbook | Developer.WordPress.org"
[2]: https://tailwindcss.com/docs/styling-with-utility-classes?utm_source=chatgpt.com "Styling with utility classes - Core concepts"
[3]: https://vite.dev/guide/assets?utm_source=chatgpt.com "Static Asset Handling"
[4]: https://www.awwwards.com/sites/lama-lama-2?utm_source=chatgpt.com "Lama Lama - Awwwards SOTD"
[5]: https://swup.js.org/options/?utm_source=chatgpt.com "Options"
[6]: https://lenis.darkroom.engineering/?utm_source=chatgpt.com "Lenis – Smooth Scroll"

